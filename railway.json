{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "php artisan config:clear && php artisan migrate --force --no-interaction; php artisan db:seed --class=ServiceSeeder --force --no-interaction; php artisan db:seed --class=AdminSeeder --force --no-interaction; php -S 0.0.0.0:${PORT:-3000} -t public",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
